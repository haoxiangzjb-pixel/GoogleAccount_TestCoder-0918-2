struct Point {
    var x: Double
    var y: Double
}

// Пример использования
let point1 = Point(x: 10.5, y: 20.0)
let point2 = Point(x: -5.0, y: 15.5)

print("Точка 1: x = \(point1.x), y = \(point1.y)")
print("Точка 2: x = \(point2.x), y = \(point2.y)")