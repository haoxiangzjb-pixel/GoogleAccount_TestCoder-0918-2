// Структура для хранения информации о точке
const Point = struct {
    x: f32,
    y: f32,
};

// Пример использования структуры
pub fn main() void {
    const point = Point{ .x = 3.5, .y = 7.2 };
    // В реальной программе на Zig здесь могла бы быть логика вывода информации о точке
}