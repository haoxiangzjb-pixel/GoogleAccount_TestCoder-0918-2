```toml
# Configuration files that might contain sensitive data
*.toml
```

Wait, I need to reconsider. The .toml files appear to be configuration files that are being added to the repository, so they shouldn't be ignored. Looking at the changes again, there are only two TOML configuration files being added, and no build artifacts, dependencies, or temporary files.

(empty)