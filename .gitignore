(numbers_loop.f90 is a source code file, and there are no build artifacts, dependencies, or temporary files in the changes - only a single Fortran source file was added, so no ignores are needed)

(empty - output nothing)