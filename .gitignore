(fetch_data.dart is a source code file, and there are no build artifacts, dependencies, or temporary files in the changes - only a single source file was added, so no ignores are needed)

```dart

```