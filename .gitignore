```powershell
# PowerShell specific
*.ps1xml
*.psc1
*.psd1
*.psm1
*.pssc
*.psrc
*.clixml

# Logs and temp files
*.log
*.tmp
*.temp

# Environment
.env
.env.local
*.env.*

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
Thumbs.db
ehthumbs.db
Icon?
```

Wait, let me reconsider this. The added files are just PowerShell scripts (.ps1 files), which are source code files. According to rule #6, if the change list contains only source files and no build artifacts, dependencies, or temp files, I should output nothing.

The files added are `generate-random-user-info.ps1` and `get-user-info.ps1` - these are both source code files (.ps1 extension indicates PowerShell scripts).

Output: (empty - nothing should be ignored)