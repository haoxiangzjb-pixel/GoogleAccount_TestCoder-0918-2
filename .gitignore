```
# Configuration files
*.yml
*.yaml

# Serverless framework
.serverless/
.sls/
.serverless_plugins/

# Environment files
.env
.env.local
.env.*

# Logs
*.log

# Temporary files
*.tmp
*~
```

Wait, let me reconsider this. The only file added is `serverless_abc123.yml`, which appears to be a configuration file that should likely be tracked in version control. Since this is just a single configuration file and not a build artifact or temporary file, I should not ignore it.

```

```