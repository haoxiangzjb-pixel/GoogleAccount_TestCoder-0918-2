# Загрузка необходимых библиотек (необязательно для базовой диаграммы рассеяния)
# library(ggplot2) # Пример для альтернативного способа

# Загрузка данных
data(mtcars)

# Построение диаграммы рассеяния
plot(mtcars$wt, mtcars$mpg,
     main = "Диаграмма рассеяния: Вес vs. MPG",
     xlab = "Вес (1000 lbs)",
     ylab = "Мили на галлон",
     pch = 19, # тип точки
     col = "blue" # цвет точки
)

# Сохранение графика в файл PNG с тем же именем, что и скрипт
png("scatter_plot_20250929_103000.png")
plot(mtcars$wt, mtcars$mpg,
     main = "Диаграмма рассеяния: Вес vs. MPG",
     xlab = "Вес (1000 lbs)",
     ylab = "Мили на галлон",
     pch = 19,
     col = "blue"
)
dev.off() # Закрытие устройства вывода