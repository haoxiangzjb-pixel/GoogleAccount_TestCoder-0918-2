# Web Scraper for Page Titles

This directory contains a Python script that uses BeautifulSoup to scrape page titles from websites.

## Generated Script

The randomly generated script is: `un46e2pn.py`

## Usage

Run the script with:

```bash
python3 {random_filename}
```

The script will scrape titles from example URLs and save the results to a text file.
