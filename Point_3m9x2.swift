struct Point {
    var x: Double
    var y: Double
}

// Пример использования
let point1 = Point(x: 1.5, y: 2.7)
let point2 = Point(x: -3.0, y: 4.1)

print("Точка 1: x = \(point1.x), y = \(point1.y)")
print("Точка 2: x = \(point2.x), y = \(point2.y)")