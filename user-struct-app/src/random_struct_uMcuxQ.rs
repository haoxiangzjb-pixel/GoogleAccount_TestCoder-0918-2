// This is a Rust source file with a randomly generated name, defining the User struct.

/// A simple struct representing a User.
#[derive(Debug)] // Added Debug for easy printing
struct User {
    /// The name of the user.
    name: String,
    /// The age of the user.
    age: u32,
}

// Example usage is commented out as this file might not be the main entry point.
/*
fn main() {
    let user = User {
        name: String::from("Bob"),
        age: 25,
    };
    println!("Created user: {:?}", user);
}
*/