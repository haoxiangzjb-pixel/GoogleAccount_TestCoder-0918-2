name: network_data_fetcher
description: A Dart program to fetch network data and save to randomly named .dart files.

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  http: ^0.13.5

dev_dependencies:
  lints: ^2.0.0