name: network_data_fetcher
description: A Dart application to fetch network data and save to random named files.

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  http: ^0.13.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0