service: my-node-service

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

functions:
  hello:
    handler: index.handler
    events:
      - http:
          path: hello
          method: get