// Определяем структуру Point
const Point = struct {
    x: f32,
    y: f32,
};

// Пример использования структуры
pub fn main() void {
    const my_point = Point{ .x = 3.14, .y = 2.71 };
    // В реальном приложении Zig здесь мог быть код для вывода или использования точки
}