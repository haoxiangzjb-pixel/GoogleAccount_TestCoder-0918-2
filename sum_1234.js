// Функция для сложения двух чисел
function sum(a, b) {
  return a + b;
}

// Экспорт функции для возможности её использования в других модулях (если нужно)
// module.exports = sum; // Для Node.js
// export default sum; // Для ES6 модулей

// Пример использования:
console.log(sum(2, 3)); // Выведет 5