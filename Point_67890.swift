struct Point {
    var x: Double
    var y: Double
}

// Пример использования
let pointA = Point(x: 3.5, y: 7.2)
let pointB = Point(x: -1.0, y: 4.8)

print("Точка A: x = \(pointA.x), y = \(pointA.y)")
print("Точка B: x = \(pointB.x), y = \(pointB.y)")